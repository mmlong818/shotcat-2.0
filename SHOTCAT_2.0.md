# Shotcat 2.0 开发基线

## 隔离边界

- 2.0 工作目录：`E:\codex\shotcat-2.0`
- 旧版只读来源：`E:\codex\shotcat`
- 旧版基线提交：`4220d4cdbd8c41ff68f9aa8e881dd48652b648c4`
- 2.0 是独立 Git 仓库，没有配置远端，不会向旧版仓库提交或推送。
- 2.0 不复制旧版 `.env`、数据库、上传文件、日志、虚拟环境或 `node_modules`。

## 2.0 产品主线

```text
项目 → 剧本 → 项目大脑 → 设定 → 分镜 → 画面 → 总览
```

所有页面围绕同一组项目、章节、镜头和资产 ID 工作。页面切换不能改变任务状态，
重新执行上游步骤必须通过版本和依赖关系明确标记受影响的下游内容。

## 第一阶段

1. 项目大脑：区分原文事实、用户决定和 AI 推断，支持确认、拒绝、锁定与版本冲突保护。
2. 统一镜头记录：补齐叙事目的、前后镜头关系、声音、特效和资产版本引用。
3. 持久任务中心：将 Pipeline 和生成任务统一为服务端项目任务，不依赖单个页面存活。
4. 依赖失效：剧本、设定或分镜改动时计算影响范围，经用户确认后使下游结果失效。
5. 导演修正：保留原稿，按问题定位修正对应镜头并记录修订版本。

## 当前已落地

- 项目大脑支持全文 AI 分析，候选先分类、去重，再形成派生约束。
- AI 候选默认待确认且未锁定；已确认或已锁定内容不会被重新分析覆盖。
- 分析任务由服务端持久化，切换或刷新页面后仍可恢复进度，并可停止或取消。
- 已确认项目规则已经进入 AI 拆镜头、导演复核、定向修正和画面提示词生成。
- 2.0 页面保留明确的“分析—审阅—确认”层级，不把自动推断直接伪装成项目事实。
- 镜头、镜头设计、画面提示词和生成图片分别记录版本；剧本、规则或分镜变化后，旧结果会明确显示为“待重做”。
- Pipeline 作业保存在服务端文件中，刷新页面、切换页面或重启浏览器后，任务中心仍可恢复运行、等待确认、失败和取消状态。
- 每次重做会先保存项目快照并计算受影响的下游数量；项目首页展示待处理步骤和最近版本，可导出 JSON 快照。
- 导演校验先落地完整草稿，再等待用户确认；继续修正时只替换命中的问题镜头，并记录独立修订版本。
- 生图任务记录生成时使用的镜头版本、提示词版本和资产引用；编辑镜头或提示词后，已有画面不会被误报为最新结果。

## 数据原则

- 用户确认并锁定的规则，自动流程不得覆盖。
- AI 推断必须默认进入待确认状态。
- 用户上传并采用的设计稿使用稳定资产 ID；改名不改变引用。
- 每个生成结果记录所使用的镜头版本、提示词版本和资产版本。
- 删除和重做必须先展示影响范围，并保留可恢复的历史记录。

## 本阶段验收标准

- 页面切换不丢 Pipeline 和生成任务状态。
- 服务重启后，已完成和等待导演确认的 Pipeline 记录仍可查询；被中断的运行任务明确标为失败，不会假装继续执行。
- 重做分镜前先显示影响数量并保存旧版本；导演修正只改问题镜头。
- 上游修改后，分镜页、画面页和项目首页同时显示失效状态。
- 版本快照覆盖项目、剧本、项目大脑、设定、镜头、对白、引用关系和生成画面记录。
